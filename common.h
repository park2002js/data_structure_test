#ifndef __COMMON__
#define __COMMON__

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

void bubSort(int* , int);
void selSort(int* , int);
void insSort(int* , int);


void swap(int*, int*);
void printArr(int*, int );

#endif